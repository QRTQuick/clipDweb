# Get these from: Vercel Dashboard → your project → Storage → Connect Store
# (Add Upstash Redis from the Marketplace, then these are auto-injected)
KV_REST_API_URL=https://YOUR-STORE.upstash.io
KV_REST_API_TOKEN=YOUR_TOKEN_HERE
